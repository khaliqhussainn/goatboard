Drop mascot images here (e.g. goat-1.png, goat-2.png, ...), then list them
in src/lib/mascots.ts's MASCOT_IMAGES array. Multiple images let the mascot
sticker rotate per campaign instead of repeating the same pose everywhere.
